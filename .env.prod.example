# .env.prod.example
# Este archivo no contiene secretos. Las variables sensibles se inyectan en el despliegue.

DB_URL=jdbc:mysql://idp.upc.edu:3306/apren?useSSL=false&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=tu_password_de_produccion

SERVER_ADDRESS=0.0.0.0
SERVER_PORT=8006

# GEMINI_API_KEY no se pone aquí. Se configura en GitLab CI/CD Variables.
